% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-precisionSim.R, R/precisionSim.R
\name{print.precisionSim}
\alias{print.precisionSim}
\alias{summary.precisionSim}
\alias{as.data.frame.precisionSim}
\alias{quantile.precisionSim}
\alias{plot.precisionSim}
\alias{precisionSim}
\title{Print a precision simulation}
\usage{
\method{print}{precisionSim}(x, digits = 2, ...)

\method{summary}{precisionSim}(object, digits = 2, ...)

\method{as.data.frame}{precisionSim}(x, row.names = NULL, optional = FALSE, ...)

\method{quantile}{precisionSim}(x, prob = x$settings$prob, ...)

\method{plot}{precisionSim}(x, type = "assurance", ...)

precisionSim(
  fit,
  target_width,
  interval_fun,
  prob = 0.8,
  mc_conf_level = 0.95,
  nsim = 1000,
  seed = 123456,
  singular_action = "include",
  nonconverged_action = "exclude",
  tol = 1e-04
)
}
\arguments{
\item{x}{An object of class `"precisionSim"`.}

\item{digits}{Number of digits used when formatting numerical results.}

\item{...}{Additional arguments. Currently unused.}

\item{object}{An object of class `"precisionSim"`.}

\item{row.names}{Default is NULL.}

\item{optional}{Default is FALSE}

\item{prob}{A single numeric value between 0 and 1 giving the target
probability of achieving the requested interval width.}

\item{type}{The type of plot, where `"assurance"` displays the
empirical cumulative distribution of confidence-interval widths,
and `"distribution"` shows a histogram of the simulated
confidence-interval widths.}

\item{fit}{A fitted linear mixed-effects model inheriting from
[lme4::lmerMod].}

\item{target_width}{A positive numeric vector giving the maximum
acceptable total confidence-interval widths.}

\item{interval_fun}{A function that accepts a fitted model and returns
a data frame containing the columns `estimand`, `estimate`, `lower`,
and `upper`.}

\item{mc_conf_level}{A single numeric value between 0 and 1 giving the
confidence level used for Monte Carlo confidence intervals.}

\item{nsim}{A positive whole number giving the number of simulations.}

\item{seed}{An optional integer seed for reproducible simulation.}

\item{singular_action}{A character value specifying how singular fits
are handled. Available options are `"include"`, `"exclude"`, and
`"failure"`.}

\item{nonconverged_action}{A character value specifying how
non-converged fits are handled. Available options are `"include"`,
`"exclude"`, and `"failure"`.}

\item{tol}{Tolerance for determining singular fit. Default is 1e-4.}
}
\value{
The input object `x`, invisibly.

The input object `x`, invisibly.

Data frame with precisionSim results

A data frame containing the estimand, requested probability,
  and corresponding confidence-interval width.

The input object `x`, invisibly.

An object of class `"precisionSim"`. The object contains:

* `results`: simulation-level interval results;
* `summary`: precision-assurance estimates;
* `diagnostics`: fitting and interval diagnostics;
* `settings`: simulation settings;
* `model`: model metadata;
* `elapsed`: elapsed computation time.
}
\description{
Calculate quantiles of the simulated confidence-interval width
distribution for each estimand.

`precisionSim()` estimates the probability that confidence intervals
for one or more user-defined estimands are narrower than specified
target widths.
}
\details{
A quantile at probability 0.80 is the width that 80 percent of the
simulated confidence intervals do not exceed.

The function repeatedly simulates a response from a fitted linear
mixed-effects model, refits the model, calculates confidence intervals
using a user-supplied function, and compares the resulting interval
widths with the requested targets.


For each simulation, a new response is generated from `fit`. The model
is then refitted and `interval_fun` is applied to the fitted model.
Total interval width is calculated as `upper - lower`.

The precision-assurance estimate is the proportion of simulated studies
in which the confidence-interval width is below `target_width`.
}
\section{Interval function}{

`interval_fun` must return a data frame with one row per estimand and
the following columns:

* `estimand`: a unique character label;
* `estimate`: the point estimate;
* `lower`: the lower confidence limit;
* `upper`: the upper confidence limit.
}

\section{Failed and excluded simulations}{

Simulations classified as `"failure"` are treated as not achieving the
target. Simulations classified as `"exclude"` are removed from the
analysable denominator. The returned object reports both the requested
and analysable denominators.
}

\examples{
\dontrun{
library(lme4)

fit <- lmer(
  response ~ task + velocity + (1 | participant),
  data = example_data
)

my_interval <- function(fitted_model) {
  # Calculate and return estimand, estimate, lower, and upper.
}

result <- precisionSim(
  fit = fit,
  interval_fun = my_interval,
  target_width = c(10, 20, 50),
  nsim = 1000,
  seed = 123
)

print(result)
summary(result)
plot(result)
}

}
\seealso{
[precisionCurve()], [simr::extend()], [stats::quantile()]
}
